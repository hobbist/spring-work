<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.2.xsd">
		<security:global-method-security secured-annotations="enabled"></security:global-method-security>
	<security:authentication-manager>
		<security:authentication-provider>
			<security:user-service>
				<security:user name="kapil" password="kapil"
					authorities="admin" />
				<security:user name="john" password="john"
					authorities="admin" />
			</security:user-service>
		</security:authentication-provider>
		<security:authentication-provider>
			<security:jdbc-user-service
				data-source-ref="dataSource" authorities-by-username-query="select * from authorities where binary username=?"
				users-by-username-query="select * from users where binary username=?" />
		</security:authentication-provider>
	</security:authentication-manager>
	<security:http use-expressions="true">
		<security:csrf disabled="true" />
		<security:intercept-url access="permitAll"
			pattern="/admin" />
<!-- 		<security:intercept-url access="hasAuthority('ROLE_ADMIN')"
			pattern="/admin" />-->
		<security:intercept-url access="isAuthenticated()"
			pattern="/createOffers" />
		<security:intercept-url access="isAuthenticated()"
			pattern="/doCreate" />
		<security:intercept-url access="isAuthenticated()"
			pattern="/offerCreated" />
		<security:intercept-url access="permitAll"
			pattern="/" />
		<security:intercept-url access="permitAll"
			pattern="/loggedout" />
		<security:intercept-url access="permitAll"
			pattern="/newAccount" />
		<security:intercept-url access="permitAll"
			pattern="/createAccount" />
		<security:intercept-url access="permitAll"
			pattern="/accountCreated" />
		<security:intercept-url access="permitAll"
			pattern="/static/**" />
		<security:intercept-url access="permitAll"
			pattern="/script/**" />
		<security:intercept-url access="permitAll"
			pattern="/login" />
		<security:intercept-url access="permitAll"
			pattern="/offers" />
		<security:intercept-url access="denyAll"
			pattern="/**" />
		<security:form-login authentication-failure-url="/login?error=true"
			login-page="/login" />
		<security:logout logout-success-url="/loggedout" />

	</security:http>
</beans>
